# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

CONTENTFUL_SETUP.md

.env
README_PROJECT.md
# Sponsor source materials (kept locally, not committed)
src/*.zip
src/*.xlsx

# Railway config-as-code (may reference secrets)
.railway/
.railway-config-pull-*/

# invite queue holds recipient PII (public repo) — never commit
uecdp/data/

# Recipient PII / source data — never commit to this public repo
UEC_MASTER_DATABASE.xlsx
PICTURES FOR EMAIL/
